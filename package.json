{
  "name": "liber-os",
  "version": "1.1.1",
  "private": true,
  "description": "Liber.OS — a haunted CRT operating system in a dark room. Art therapy tool bridging perennial psychology, archetypes, and shadow work. Offline, no build step, no framework.",
  "scripts": {
    "start": "node serve.cjs",
    "dev": "node serve.cjs",
    "smoke": "node smoke.mjs",
    "shoot": "node shoot.mjs",
    "app:dist": "node scripts/app-dist.cjs",
    "app:dev": "npm run app:dist && tauri dev",
    "app:build": "npm run app:dist && touch src-tauri/src/main.rs src-tauri/src/lib.rs && tauri build"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "playwright": "1.62.1"
  }
}
